import styled from "styled-components";
import tw from "twin.macro";

const H2Subtitle = styled.h2`
    ${tw`text-[18px] px-10 py-4 text-black font-bold`}
    ${tw`mobile:text-[12px] mobile:px-5 mobile:py-2`}
`

export default H2Subtitle;