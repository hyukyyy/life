import styled from "styled-components";
import tw from "twin.macro";

const H3TabTitle = styled.h3`
    ${tw`text-[32px] px-10 py-4 text-black border-b-2 border-black mb-4 cursor-pointer relative z-10 bg-white font-extrabold`}
    ${tw`mobile:text-[24px] mobile:px-5 mobile:py-2`}
`

export default H3TabTitle